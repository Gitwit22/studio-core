/**
 * Demo Landing Toggle
 *
 * When `true`  → "/" shows the Demo switchboard (Creator / EDU / Corporate).
 * When `false` → "/" shows the original StreamLine Welcome page.
 *
 * Flip this single flag to enable or disable the demo landing.
 */
export const DEMO_LANDING_ENABLED = false;
